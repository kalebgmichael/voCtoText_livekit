# GPU overrides — applied on top of docker-compose.yml by compose-up.sh when GPU mode is selected.
# The base docker-compose.yml already targets GPU. This file is kept for compose-up.sh compatibility
# and can be used to add GPU-only services (e.g. llama_cpp, nemotron) in the future.
services: {}
